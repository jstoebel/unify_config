---

- name: Install php packages
  apt:
    name: "{{item}}"
    state: installed
    update_cache: yes

  with_items:
    - php-fpm
    - php-mysql
    - php-curl
    - php-gd
    - php-mbstring
    - php-mcrypt
    - php-xml
    - php-xmlrpc

  notify:
    - "copy php.ini"
    - restart php
